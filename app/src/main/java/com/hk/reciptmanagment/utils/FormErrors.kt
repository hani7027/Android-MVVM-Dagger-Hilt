package com.hk.reciptmanagment.utils

enum class FormErrors {
    MISSING_FIRST_NAME,
    MISSING_LAST_NAME,
    INVALID_PHONE_NUMBER,
    INVALID_USERNAME,
    INVALID_PASSWORD,
    PASSWORDS_NOT_MATCHING,
}